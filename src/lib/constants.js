const MERGE_STRATEGIES = {
  DEEP: 'deep',
  SPREAD: 'spread',
  REPLACE: 'replace',
};

const DEFAULT_MERGE_STRATEGY = MERGE_STRATEGIES.DEEP;

module.exports = {
  MERGE_STRATEGIES,
  DEFAULT_MERGE_STRATEGY,
};
